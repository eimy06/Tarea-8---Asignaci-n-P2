import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.registro.app',
  appName: 'war_20209190',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
